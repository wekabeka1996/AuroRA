[mutmut]
# Limit mutation to core aurora logic
paths_to_mutate=src/aurora/execution,src/aurora/risk,src/aurora/governance
# Use pytest as runner; keep it quiet for speed
runner=pytest -q
# Exclude obvious non-code or heavy modules
backup=False
use_coverage=False

# Timeouts (seconds)
timeout_factor=1.5
