"""Archived top-level composite_sprt tests (renamed to avoid pytest module name collision).

If you need the full tests here, merge with tests/unit/test_composite_sprt.py and reintroduce with a unique filename.
"""
